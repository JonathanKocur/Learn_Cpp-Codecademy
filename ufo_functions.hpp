#include <vector>

// Here are all of the function declarations 
void display_misses(int misses);
void display_status (std::vector<char> incorrect, std::string answer);
void greet();
void end_game(std::string answer, std::string codeword);